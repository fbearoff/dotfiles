# Can be moved to XDG_CONFIG  with this PR https://github.com/r-lib/lintr/pull/460

linters: linters_with_defaults(
    line_length_linter(120),
    commented_code_linter = NULL,
    trailing_whitespace_linter = NULL
  )
